# Supabase - Contact form backend
# Get these from your Supabase project: https://app.supabase.com
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Required Supabase tables:
# Run supabase/migrations/001_booking_system.sql in the SQL Editor
# for booking system tables (availability_rules, bookings, blocked_slots).
#
# For the contact form, also run:
# create table contact_submissions (
#   id uuid default gen_random_uuid() primary key,
#   name text not null,
#   email text not null,
#   message text not null,
#   created_at timestamptz default now()
# );
#
# alter table contact_submissions enable row level security;
#
# create policy "Allow anonymous inserts"
#   on contact_submissions for insert
#   with check (true);

# SMTP - Email notifications
# Uses your domain's email SMTP server (e.g. GoDaddy, Google Workspace)
SMTP_USER=aravind@ohboyconsultancy.com
SMTP_PASSWORD=
